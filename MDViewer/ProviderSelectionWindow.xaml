<Window x:Class="it.carpanese.utilities.MDViewer.ProviderSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:it.carpanese.utilities.MDViewer"
        Title="{local:Loc ProviderDialogTitle}" Height="500" Width="780"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}"
        Icon="pack://application:,,,/MDViewer;component/Resources/md-viewer-icon.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{local:Loc ProviderDialogIntro}"
                   Style="{StaticResource SecondaryTextStyle}"
                   Margin="16,16,16,10"/>

        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="290"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox x:Name="ProvidersList"
                     Grid.Column="0"
                     SelectionChanged="ProvidersList_SelectionChanged"
                     BorderBrush="{DynamicResource ControlBorderBrush}"
                     BorderThickness="1"
                     Background="{DynamicResource ControlBackground}"
                     Foreground="{DynamicResource TextForeground}">
                <ListBox.Resources>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="2"/>
                        <Setter Property="Margin" Value="0,0,0,2"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ControlPressedBackground}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.Resources>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="8" CornerRadius="5">
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayName}"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextForeground}"/>
                                <TextBlock Text="{Binding ShortDescription}"
                                           Margin="0,3,0,0"
                                           FontSize="11"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource TextForegroundSecondary}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Column="2"
                    Background="{DynamicResource PanelBackground}"
                    BorderBrush="{DynamicResource PanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="{local:Loc ProviderDialogDetailsHeader}"
                                   Style="{StaticResource SettingLabelTextStyle}"/>
                        <TextBlock x:Name="TxtProviderName"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Margin="0,8,0,8"
                                   Foreground="{DynamicResource AccentColor}"/>
                        <TextBlock x:Name="TxtProviderDescription"
                                   TextWrapping="Wrap"
                                   LineHeight="22"
                                   Foreground="{DynamicResource TextForeground}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Background="{DynamicResource PanelBackground}"
                BorderBrush="{DynamicResource PanelBorderBrush}"
                BorderThickness="1,1,1,0"
                Padding="24,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel"
                        Content="{local:Loc ProviderDialogCancel}"
                        Width="90"
                        Height="32"
                        Margin="0,0,8,0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnCancel_Click"/>
                <Button x:Name="BtnUseProvider"
                        Content="{local:Loc ProviderDialogUseProvider}"
                        Width="120"
                        Height="32"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="BtnUseProvider_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
